#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"

exec cargo run --quiet --manifest-path "${REPO_DIR}/Cargo.toml" --bin drive_manager -- "$@"
